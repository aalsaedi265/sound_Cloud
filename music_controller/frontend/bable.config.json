


{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": {
          "node": "10"
        }
      }
    ],
    "@babel/preset-react" // Include the preset for React JSX
  ],
  "plugins": [
    "@babel/plugin-proposal-class-properties" // Optional plugin for class properties
  ]
}
